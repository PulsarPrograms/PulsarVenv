#include <string>
#include <vector>
#include <chrono>

using namespace std;

void clear_screen();


string get_current_date_time();

vector<std::string> split(const string& str);

string remove_quotes(string str);