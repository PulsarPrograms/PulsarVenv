#include <string>
#include <vector>
#include <chrono>

using namespace std;

void clear_screen();


string getCurrentDateTime();

vector<std::string> split(const string& str);

string remove_quotes(string str);